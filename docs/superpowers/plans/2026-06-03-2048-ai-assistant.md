# 2048 AI 助手实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 在 2048 游戏页面右侧添加 AI 面板，用户可输入 DeepSeek API Key 获取棋局建议。

**Architecture:** 新建 `GameAiPanel` 独立组件，接收 `board` 和 `score` 作为 props，在 page.tsx 中引入并放置于棋盘右侧。AI 逻辑（API 调用、状态管理、localStorage 持久化）完全封装在组件内部。

**Tech Stack:** React 19, TypeScript, shadcn/ui (Button, Input), fetch + AbortController, localStorage

---

## 文件改动

| 操作 | 文件 | 说明 |
|------|------|------|
| 新建 | `src/components/game-2048-ai-panel.tsx` | AI 面板组件 |
| 修改 | `src/app/game/2048/page.tsx:159-165` | 引入 GameAiPanel 并布局 |

---

### Task 1: 创建 GameAiPanel 组件

**文件:** 新建 `src/components/game-2048-ai-panel.tsx`

- [ ] **Step 1: 编写组件接口和基础结构**

```tsx
"use client"

import { useState, useCallback, useRef, useEffect } from "react"
import { Button } from "@/components/ui/button"
import { Input } from "@/components/ui/input"
import type { TileBoard, Direction } from "@/lib/game-2048"

// ---- 常量 ----
const DEEPSEEK_URL = "https://api.deepseek.com/v1/chat/completions"
const DEEPSEEK_MODEL = "deepseek-chat"
const LOCAL_KEY = "2048_deepseek_api_key"
const LOCAL_AUTO = "2048_ai_auto_mode"

// ---- 类型 ----
interface AiMessage {
  boardSnapshot: number[][]  // 4×4 棋盘快照
  direction: Direction
  reason: string
  timestamp: number
}

interface DeepSeekResponse {
  direction: Direction
  reason: string
}

interface GameAiPanelProps {
  board: TileBoard | null
  score: number
}
```

- [ ] **Step 2: 实现组件主干 (状态定义 + 初始化)**

在同一个文件中，添加组件函数：

```tsx
export default function GameAiPanel({ board, score }: GameAiPanelProps) {
  // ---- 状态 ----
  const [apiKey, setApiKey] = useState("")
  const [autoMode, setAutoMode] = useState(false)
  const [messages, setMessages] = useState<AiMessage[]>([])
  const [loading, setLoading] = useState(false)
  const [error, setError] = useState<string | null>(null)

  // ---- ref（用于 AbortController） ----
  const abortRef = useRef<AbortController | null>(null)

  // ---- 从 localStorage 恢复 ----
  useEffect(() => {
    try {
      const savedKey = localStorage.getItem(LOCAL_KEY)
      if (savedKey) setApiKey(savedKey)
      const savedAuto = localStorage.getItem(LOCAL_AUTO)
      if (savedAuto === "true") setAutoMode(true)
    } catch { /* localStorage 不可用时静默降级 */ }
  }, [])

  // ---- 保存到 localStorage ----
  const handleApiKeyChange = useCallback((val: string) => {
    setApiKey(val)
    try { localStorage.setItem(LOCAL_KEY, val) } catch { /* 静默降级 */ }
  }, [])

  const toggleAutoMode = useCallback(() => {
    setAutoMode((prev) => {
      const next = !prev
      try { localStorage.setItem(LOCAL_AUTO, String(next)) } catch { /* 静默降级 */ }
      return next
    })
  }, [])
```

- [ ] **Step 3: 实现棋盘序列化函数**

```tsx
  /** 将 TileBoard 序列化为 4×4 数字矩阵（null → 0） */
  function serializeBoard(b: TileBoard): number[][] {
    return b.map((row) =>
      row.map((t) => (t ? t.value : 0))
    )
  }
```

- [ ] **Step 4: 实现 prompt 构建函数**

```tsx
  /** 构建发往 DeepSeek 的 prompt */
  function buildPrompt(b: TileBoard, s: number): string {
    const grid = serializeBoard(b)
      .map((row) => `[${row.join(",")}]`)
      .join("\n")

    return `你是一个 2048 游戏 AI 助手，请分析当前棋局并给出最佳移动方向。
只回复 JSON 格式，不要任何其他文字：
{"direction": "up|down|left|right", "reason": "中文理由，一句话"}

当前棋盘（4×4，0=空格）：
${grid}

当前分数: ${s}`
  }
```

- [ ] **Step 5: 实现 DeepSeek API 调用**

```tsx
  /** 调用 DeepSeek API 获取建议 */
  async function fetchSuggestion(b: TileBoard, s: number): Promise<DeepSeekResponse> {
    const controller = new AbortController()
    abortRef.current = controller

    const res = await fetch(DEEPSEEK_URL, {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        Authorization: `Bearer ${apiKey}`,
      },
      body: JSON.stringify({
        model: DEEPSEEK_MODEL,
        messages: [
          { role: "user", content: buildPrompt(b, s) },
        ],
        temperature: 0.3,
        max_tokens: 128,
      }),
      signal: controller.signal,
    })

    if (!res.ok) {
      if (res.status === 401) throw new Error("API Key 无效，请检查")
      if (res.status === 429) throw new Error("请求过于频繁，请稍后重试")
      throw new Error(`API 错误 (${res.status})`)
    }

    const data = await res.json()
    const content = data.choices?.[0]?.message?.content?.trim()
    if (!content) throw new Error("AI 返回为空，请重试")

    // 尝试提取 JSON（兼容 AI 可能输出额外文字）
    const jsonMatch = content.match(/\{.*\}/s)
    if (!jsonMatch) throw new Error("AI 返回格式异常，请重试")

    const parsed = JSON.parse(jsonMatch[0]) as DeepSeekResponse

    // 验证 direction 合法性
    const validDirs: Direction[] = ["up", "down", "left", "right"]
    if (!validDirs.includes(parsed.direction)) {
      throw new Error("AI 返回了无效方向，请重试")
    }

    return parsed
  }
```

- [ ] **Step 6: 实现 getSuggestion 主逻辑**

```tsx
  /** 获取建议（供手动按钮和自动模式共用） */
  const getSuggestion = useCallback(async () => {
    if (!board || !apiKey.trim()) return
    if (loading) return // 防止重复请求

    // 取消上一次未完成的请求
    if (abortRef.current) {
      abortRef.current.abort()
    }

    setLoading(true)
    setError(null)

    try {
      const snapshot = serializeBoard(board)
      const result = await fetchSuggestion(board, score)

      const msg: AiMessage = {
        boardSnapshot: snapshot,
        direction: result.direction,
        reason: result.reason,
        timestamp: Date.now(),
      }
      setMessages((prev) => [...prev, msg])
    } catch (e) {
      if (e instanceof DOMException && e.name === "AbortError") {
        // 被取消，忽略
        return
      }
      setError(e instanceof Error ? e.message : "未知错误")
    } finally {
      setLoading(false)
    }
  }, [board, score, apiKey, loading])
```

- [ ] **Step 7: 实现自动建议（监听 board 变化）**

```tsx
  // 自动建议：当 board 变化且 autoMode 开启时触发
  const prevBoardRef = useRef(board)
  const initializedRef = useRef(false)

  useEffect(() => {
    // 首次渲染不触发（避免开局就请求）
    if (!initializedRef.current) {
      initializedRef.current = true
      prevBoardRef.current = board
      return
    }

    // board 没有实际变化时不触发
    if (prevBoardRef.current === board) return
    prevBoardRef.current = board

    if (autoMode && board) {
      getSuggestion()
    }
  }, [board, autoMode, getSuggestion])
```

- [ ] **Step 8: 实现 UI 渲染**

```tsx
  // ---- 方向映射为箭头符号 ----
  const DIR_ARROW: Record<Direction, string> = {
    up: "↑ (向上)",
    down: "↓ (向下)",
    left: "← (向左)",
    right: "→ (向右)",
  }

  return (
    <div
      className="flex w-[300px] shrink-0 flex-col gap-4 rounded-lg border bg-white p-4"
      style={{ minHeight: BOARD_PX }}
    >
      {/* ---- API Key 输入 ---- */}
      <div className="flex flex-col gap-1.5">
        <label className="text-xs font-medium" style={{ color: "#6A7282" }}>
          🔑 DeepSeek API Key
        </label>
        <Input
          type="password"
          placeholder="sk-xxxxxxxxxxxxxxxx"
          value={apiKey}
          onChange={(e) => handleApiKeyChange(e.target.value)}
        />
      </div>

      {/* ---- 自动建议开关 + 手动按钮 ---- */}
      <div className="flex items-center gap-2">
        <label className="flex cursor-pointer items-center gap-1.5 text-xs">
          <input
            type="checkbox"
            checked={autoMode}
            onChange={toggleAutoMode}
            className="h-3.5 w-3.5"
          />
          自动建议
        </label>
        <div className="flex-1" />
        <Button
          variant="default"
          size="sm"
          onClick={getSuggestion}
          disabled={!apiKey.trim() || !board || loading}
        >
          {loading ? "分析中…" : "获取建议"}
        </Button>
      </div>

      {/* ---- 错误提示 ---- */}
      {error && (
        <div className="rounded-md bg-red-50 p-2 text-xs" style={{ color: "#DC2626" }}>
          {error}
        </div>
      )}

      {/* ---- 最新建议 ---- */}
      {messages.length > 0 && (
        <div className="flex flex-col gap-2">
          <span className="text-xs font-semibold" style={{ color: "#101828" }}>
            🤖 AI 建议
          </span>
          <div
            className="rounded-md p-3"
            style={{ backgroundColor: "#F9FAFB", border: "1px solid #E5E7EB" }}
          >
            <p className="text-sm font-bold" style={{ color: "#101828" }}>
              推荐: {DIR_ARROW[messages[messages.length - 1].direction]}
            </p>
            <p className="mt-1 text-xs" style={{ color: "#6A7282" }}>
              {messages[messages.length - 1].reason}
            </p>
          </div>
        </div>
      )}

      {/* ---- 对话历史 ---- */}
      {messages.length > 1 && (
        <div className="flex flex-col gap-2">
          <span className="text-xs font-semibold" style={{ color: "#101828" }}>
            📜 历史记录
          </span>
          <div className="flex max-h-[240px] flex-col gap-1.5 overflow-y-auto">
            {messages.map((msg, i) => (
              <div
                key={msg.timestamp}
                className="rounded-md p-2 text-xs"
                style={{ backgroundColor: "#F9FAFB" }}
              >
                <span className="font-medium" style={{ color: "#6A7282" }}>
                  第 {i + 1} 步:
                </span>{" "}
                <span style={{ color: "#101828" }}>
                  {DIR_ARROW[msg.direction]}
                </span>
                <span className="ml-1" style={{ color: "#6A7282" }}>
                  — {msg.reason}
                </span>
              </div>
            ))}
          </div>
        </div>
      )}
    </div>
  )
}
```

---

### Task 2: 集成到 page.tsx

**文件:** 修改 `src/app/game/2048/page.tsx`

- [ ] **Step 1: 添加 import GameAiPanel**

在 page.tsx 顶部，现有 import 之后添加：

```tsx
import GameAiPanel from "@/components/game-2048-ai-panel"
```

- [ ] **Step 2: 用 flex 容器包裹棋盘和 AI 面板**

找到 page.tsx 中的主 JSX 容器（`<div className="mx-auto max-w-[1280px] px-4 py-8">` 开始），将其内部的棋盘区域和标题修改为左右并排布局。具体替换以下部分：

**第一步** — 将页面标题行外的整个内容（从棋盘开始到结束）包裹在一个 flex 容器中：

```tsx
return (
  <div className="mx-auto max-w-[1280px] px-4 py-8">
    <div className="mb-6 flex items-center justify-between">
      <h1 style={{ color: "#101828", fontSize: 30, fontWeight: 700 }}>2048</h1>
      <div className="flex gap-3">
        <div className="rounded-md px-4 py-2 text-white" style={{ backgroundColor: "#BBADA0" }}>
          得分: {score}
        </div>
      </div>
    </div>

    <p className="mb-6 text-sm" style={{ color: "#6A7282" }}>
      使用方向键移动数字方块，合并相同数字，挑战 2048！
    </p>

    {/* 棋盘 + AI 面板左右并排 */}
    <div className="flex items-start gap-6">
      {/* 棋盘区域 */}
      <div
        className="relative shrink-0"
        style={{ width: BOARD_PX, height: BOARD_PX, backgroundColor: "#BBADA0", borderRadius: 8 }}
      >
        {/* ... 棋盘内部内容保持不变 ... */}
      </div>

      {/* AI 面板 */}
      <GameAiPanel board={board} score={score} />
    </div>

    {/* ... 按钮区域 ... */}
  </div>
)
```

- [ ] **Step 3: 验证构建**

```bash
pnpm lint
```

---

## 自检清单

- [ ] **Spec 覆盖**: 所有设计文档中的要求均已覆盖（API Key 输入 ✓, 自动/手动模式 ✓, 对话历史 ✓, 错误处理 ✓, prompt 模板 ✓, AbortController ✓, localStorage ✓）
- [ ] **占位符检查**: 所有步骤包含完整代码
- [ ] **类型一致性**: `Direction`, `TileBoard`, `DeepSeekResponse` 类型在组件间保持一致
